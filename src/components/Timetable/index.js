import React from 'react'

function Timetable() {
    function addEvent(){
        //first make an api call 
        //that will be post request for adding --> C
        
    }
  return (
    <div>
        <h1>Timetable</h1>
        <button onClick = {()=>{addEvent()}}>Add Event </button>
    </div>
  )
}

export default Timetable